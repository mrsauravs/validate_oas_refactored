name: streamlit-refactored
channels:
  - conda-forge
dependencies:
  # System Dependencies
  - git
  - nodejs=20
  - npm
  
  # Python Base
  - python=3.10
  - pip
  
  # Python Libraries (Install via pip for speed/stability)
  - pip:
    - streamlit
    - requests
    - pyyaml
    - python-dotenv
    - google-genai
